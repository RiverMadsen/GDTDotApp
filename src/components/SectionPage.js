import SettingsContext from "./SyncContext";

export function SectionPage({ settings, handleSettingsChange }) {
  return (
    <div>
      <h2>SectionPage Page</h2>

    </div>
  );
}
export default SectionPage;